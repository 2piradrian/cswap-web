---
import style from "./style.module.css";

type Props = {
	title: string;
	modifier?: string;
};

const {title, modifier = ""} = Astro.props;
---

<h2 class={`${style.title} ${modifier}`}>{title}</h2>
